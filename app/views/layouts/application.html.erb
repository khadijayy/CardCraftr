<!DOCTYPE html>
<html>
  <head>

    <title><%= content_for(:title) || "Cardcraftr" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%= javascript_importmap_tags %>
    <%= stylesheet_link_tag "auth_forms", media: "all" %>

  </head>

  <body>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>

    <nav class="navbar">
      <% if user_signed_in? %>
        <%= link_to "Profile", edit_user_registration_path, class: "btn" %>
        <%= link_to "Logout", destroy_user_session_path, data: { turbo_method: :delete }, class: "btn" %>
      <% end %>
    </nav>

    <%= yield %>
  </body>
</html>
